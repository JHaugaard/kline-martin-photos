/**
 * PostCSS Configuration
 *
 * Tailwind CSS v4 uses the @tailwindcss/postcss plugin
 * which handles all CSS processing automatically.
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}
